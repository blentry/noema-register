# Noema registry

Reviewed, immutable package release metadata. Package sources live at
https://github.com/blentry/noema-packages. `index.json` is generated from
`releases/`; evidence is scoped to exact content and tools. A valid release
does not establish arbitrary composition compatibility or physical correctness.
